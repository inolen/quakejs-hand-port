define('ui/ui',
[
	'underscore',
	'backbone',
	'text!ui/css/views.css',
	'ui/views/HudView',
	'ui/views/ScoreboardView',
	'ui/views/IngameMenu',
	'ui/views/SinglePlayerMenu',
	'ui/views/SettingsMenu'
],
function (_, Backbone, viewsCss, HudView, ScoreboardView, IngameMenu, SinglePlayerMenu, SettingsMenu) {
	<%- include ui-defines.js %>

	function UserInterface(sys, com, cl, r) {
		<%- include ../shared/shared.js %>
		<%- include ui-main.js %>
		<%- include ui-image.js %>

		return {
			Init:            Init,
			KeyPressEvent:   KeyPressEvent,
			MouseMoveEvent:  MouseMoveEvent,
			SetActiveMenu:   SetActiveMenu,
			CloseActiveMenu: CloseActiveMenu,
			RenderView:      RenderView,
			Render:          Render
		};
	}

	return {
		CreateInstance: function (sys, com, cl) {
			return new UserInterface(sys, com, cl);
		}
	};
});