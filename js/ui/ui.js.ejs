define('ui/ui',
[
	'underscore',
	'backbone',
	'text!ui/css/views.css',
	'ui/views/HudView',
	'ui/views/ScoreboardView',
	'ui/views/IngameMenu',
	'ui/views/SinglePlayerMenu'
],
function (_, Backbone, viewsCss, HudView, ScoreboardView, IngameMenu, SinglePlayerMenu) {
	<%- include ui-defines.js %>

	function UserInterface(sys, cl, r) {
		<%- include ui-main.js %>

		return {
			Init:            Init,
			KeyPressEvent:   KeyPressEvent,
			MouseMoveEvent:  MouseMoveEvent,
			SetActiveMenu:   SetActiveMenu,
			CloseActiveMenu: CloseActiveMenu,
			RenderView:      RenderView,
			Render:          Render
		};
	}

	return {
		CreateInstance: function (sysinterface, clinterface, rinterface) {
			return new UserInterface(sysinterface, clinterface, rinterface);
		}
	};
});