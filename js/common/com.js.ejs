define('common/com', ['server/sv', 'client/cl', 'shared/cmd'], function (sv, cl, cmd) {
	<%- include com-defines.js %>
	<%- include com-main.js %>

	return {
		EventTypes: EventTypes,

		Init: Init,
		Frame: Frame,
		QueueEvent: QueueEvent
	};
});