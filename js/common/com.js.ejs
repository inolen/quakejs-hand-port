define('common/com', ['server/sv', 'client/cl'], function (sv, cl) {
	<%- include com-defines.js %>
	<%- include com-main.js %>

	return {
		EventTypes: EventTypes,

		Init: Init,
		Frame: Frame,
		QueueEvent: QueueEvent
	};
});