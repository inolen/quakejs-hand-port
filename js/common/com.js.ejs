define('common/com',
['underscore', 'ByteBuffer', 'server/sv', 'client/cl', 'shared/cmd'],
function (_, ByteBuffer, sv, cl, cmd) {
	<%- include ../shared/shared.js %>
	<%- include com-defines.js %>
	<%- include com-main.js %>
	<%- include com-net.js %>

	return {
		EventTypes: EventTypes,

		Init:           Init,
		Frame:          Frame,
		QueueEvent:     QueueEvent,
		NetchanSetup:   NetchanSetup,
		NetchanDestroy: NetchanDestroy,
		NetchanSend:    NetchanSend,
		NetchanPrint:   NetchanPrint
	};
});