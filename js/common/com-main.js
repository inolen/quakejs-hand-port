define('common/com-main', [], function () {
	return function () {
		var com = this;

		return {
			MapLoading: function () {
				//this.clc.servername = 'localhost';
				//Q_strncpyz( clc.servername, "localhost", sizeof(clc.servername) );
				//NET_StringToAdr( clc.servername, &clc.serverAddress, NA_UNSPEC);
			}
		};
	};
});