/*global vec3: true */

define('sound/snd',
['underscore', 'glmatrix', 'common/qshared', 'common/asset-cache', 'common/cvar'],
function (_, glmatrix, QS, AssetCache, Cvar) {
	function Sound(imp) {
		var SYS = imp.SYS;
		var CL  = imp.CL;

		<% include snd-defines.js %>
		<% include snd-main.js %>
		<% include snd-cvar.js %>

		return {
			Init:                 Init,
			Shutdown:             Shutdown,
			Frame:                Frame,
			RegisterSound:        RegisterSound,
			StartSound:           StartSound,
			StartLocalSound:      StartLocalSound,
			StartBackgroundTrack: StartBackgroundTrack,
			AddLoopingSound:      AddLoopingSound,
			Respatialize:         Respatialize,
			UpdateEntityPosition: UpdateEntityPosition
		};
	}

	return Sound;
});
